[package]
name = "debate-flow"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "debate_flow_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
walkdir = "2"
# Blocking HTTP client for the CardMirror loopback bridge (127.0.0.1 only, no TLS).
ureq = { version = "2", default-features = false, features = ["json"] }
# Tiny loopback HTTP server so Nimbus is a discoverable CardMirror "flow app".
tiny_http = "0.12"
# Cryptographic randomness for the per-session bridge token.
getrandom = "0.2"

